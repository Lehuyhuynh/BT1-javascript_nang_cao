// Xây dựng class Product gồm 3 thuộc tính: name, price, amount.
class Product (name, price, amount){
  constructor(name, price, amount){
    this.name=name;
    this.price=price;
    this.amount=amount
  }
  // Product có phương thức addProduct.
  addProduct(product, quantity){
    
  }
}
// Xây dựng class Customer name, quantityOfProducts.
class Customer(name, quantityOfProducts){
  constructor(name, quantityOfProducts){
    this.name=name;
    this.quantityOfProducts=quantityOfProducts;
  }

  // Customer có phương thức buyProduct
  buyProduct(){
    if(quantity.product.amount)
    return

    prompt.amount=prompt.amount+quantity
    this.quantityOfProducts =this.quantityOfProducts +quantity
  console.log("sjfnsf"); 
 }
}